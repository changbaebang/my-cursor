# Sanitization notice

Public mirror of a private Cursor workflow repo. Internal deploy/standup/incident paths omitted.

Before push, scan for real tokens and company-specific hosts (Slack/Jira/GitHub org defaults).

Do not commit `mcp.json` or `.env` with real tokens.
